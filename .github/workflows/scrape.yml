name: Tri Hourly Scraper

on:
  schedule:
    - cron: '30 0 * * *'    # 06:00 IST
    - cron: '30 3 * * *'    # 09:00 IST
    - cron: '30 6 * * *'    # 12:00 IST
    - cron: '30 9 * * *'    # 15:00 IST
    - cron: '30 12 * * *'   # 18:00 IST
    - cron: '30 15 * * *'   # 21:00 IST
    - cron: '30 18 * * *'   # 00:00 IST
    - cron: '30 21 * * *'   # 03:00 IST
  workflow_dispatch:

jobs:
  scrape-and-commit:
    runs-on: ubuntu-latest
    timeout-minutes: 55

    permissions:
      contents: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - name: Install system dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends \
            xvfb \
            libasound2t64 \
            libatk-bridge2.0-0 \
            libdrm2 \
            libxcomposite1 \
            libxdamage1 \
            libxrandr2 \
            libgbm1 \
            libxkbcommon0 \
            libnss3 \
            libpango-1.0-0 \
            libpangocairo-1.0-0 \
            fonts-liberation \
            wget \
            gnupg

      - name: Install Python dependencies
        run: |
          pip install --upgrade pip
          pip install seleniumbase

      - name: Install Google Chrome + Chromedriver
        run: |
          wget -q -O - https://dl.google.com/linux/linux_signing_key.pub | sudo apt-key add -
          echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" | sudo tee /etc/apt/sources.list.d/google-chrome.list
          sudo apt-get update -qq
          sudo apt-get install -y google-chrome-stable
          google-chrome --version

          # Use "stable" for better compatibility with UC mode
          seleniumbase get chromedriver stable

      - name: Run scraper
        run: python scraper.py

      - name: Commit and push updated data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add docs/rev.json docs/deleted.json

          if git diff --cached --quiet; then
            echo "No changes to commit — no new/deleted reviews this run."
            exit 0
          fi

          NOW=$(date -u '+%Y-%m-%d %H:%M UTC')
          LIVE=$(python -c "import json; d=json.load(open('docs/rev.json')); print(len(d))" || echo 0)
          DEL=$(python -c "import json; d=json.load(open('docs/deleted.json')); print(len(d))" || echo 0)

          git commit -m "data: ${NOW} | live=${LIVE} deleted=${DEL}"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: scraper-failure-${{ github.run_id }}
          path: "*.log"
          retention-days: 7
